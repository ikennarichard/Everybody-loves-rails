<div class='card'>
  <img src=<%=user.photo%> alt=<%="headshot-of-#{user.name}"%> class='user_image'>
  <div class='user_details'>
    <a href='/users/<%=user.id%>'>
    <h2><%=user.name %></h2>
    </a> 
    <p>Number of Posts: <%= user.posts.length %></p>
  </div>
</div>
